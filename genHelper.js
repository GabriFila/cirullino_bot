// const areThereAces = board => {
//   let exit = false;
//   board.forEach(card => {
//     if (card.charAt(0) == "A") {
//       console.log("found ace in board");
//       exit = true;
//     }
//   });

//   return board.some(card => card.charAt(0) == "A");
// };

// module.exports.areThereAces = areThereAces;

// // ANCHOR hint
// //to have separate buttons in keyboard for cards in hand
// //[["one"], ["two", "three"]]
